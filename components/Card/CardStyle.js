import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  cardcontainer: {
    flexDirection: "column",
    gap: 3,
    marginBottom: 5,
  },
  img: {
    width: 170,
    height: 200,
  },
});
