import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  container: {
    borderWidth: 1,
    padding: 7,
    borderRadius: 3,
  },
  input: {
    fontFamily: "Literata-Medium",
  },
});
